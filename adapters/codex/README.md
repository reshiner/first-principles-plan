# Codex CLI Adapter

Entry point for Codex CLI (`~/.codex/skills/first-principles-plan/`).

- `fpp.agent.md` — Agent definition in Codex CLI's `.agent.md` format

Installed via: `bash install.sh --codex`  
This creates a symlink at `~/.codex/skills/first-principles-plan/` → `adapters/codex/`.